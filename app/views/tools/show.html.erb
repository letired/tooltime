<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-4 col-sm-offset-4">
      <h2><%= @tool.name %></h2>
      <h4>Owner: <%= @tool.user.first_name + " " + @tool.user.last_name%></h4>
      <%= render 'form' %>
      <%= link_to 'Back', tools_path, class: "btn" %>
    </div>
  </div>
</div>
