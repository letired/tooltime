<div class="banner" style="background: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('https://placeholdit.imgix.net/~text?txtsize=33&txt=350%C3%97150&w=350&h=150');">
  <div class="banner-content">
    <h1>TOOLTIME</h1>
    <p>Find the tool you need. Every time.</p>

        <%= simple_form_for(@tools), wrapper: :inline_form do |f| %>
            <%= f.input :name, label: false, placeholder: "What tool do you need?" %>
            <%= f.input :location, label: false, placeholder: "Where are you?" %>

            <!-- for 'collection:' below, define @categories in the pages#home action -->
            <%= f.input :category, label: false, collection: @categories, prompt: "Tool category" %>
            <!--  -->

            <%= f.button :submit, class: "btn btn-primary" %>
        <% end %>
  </div>
</div>
